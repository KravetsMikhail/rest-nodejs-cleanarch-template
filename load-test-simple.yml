config:
  target: 'http://localhost:1234'
  phases:
    - duration: 60
      arrivalRate: 10
      name: "Initial test"

scenarios:
  - name: "Health check"
    flow:
      - get:
          url: "/api/v1/tasks"
